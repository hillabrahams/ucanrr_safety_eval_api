OPEN_API_KEY=***REMOVED_API_KEY***

# Fallback SASI user_id used when a request arrives without a user_id (e.g. batch
# CSV runs via ui_simulator2_sasi.py).  Production traffic must always supply a
# real per-user pseudonymous ID; this value is for local/batch testing only.
# SASI_DEFAULT_USER_ID=local_batch_test